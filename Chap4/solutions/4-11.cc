#include <iostream>

using namespace std;

int main(int argc, char const *argv[])
{
    int a = 0, b = -1, c = -2, d = -3;
    if (a > b && b > c && c > d) cout << "a: " << a << ", b: " << b << "," << endl;
    return 0;
}

