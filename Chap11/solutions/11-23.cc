#include <vector>
#include <map>
#include <iostream>
#include <vector>

using namespace std;

int main(int argc, char const *argv[]) {
  multimap<string, vector<string>> family;
  family.insert({"He", vector<string>{"Lin He"}});
  return 0;
}

